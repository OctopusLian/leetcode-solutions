int* sortArrayByParity(int* A, int ASize, int* returnSize) {
    int len = ASize - 1;
    for(int i = 0;i < len;i++){
        if( A[i] % 2 == 1){
            // swap(A[i--],A[len--]);  //互换    
                        
        }
    }
    return A;
}

/*
问题一：
implicit declaration of function 'swap'

*/
